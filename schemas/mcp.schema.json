{
  "$defs": {
    "McpOauth": {
      "description": "OAuth client configuration for a remote MCP server.\n\nPublic-client fields only. A client secret is deliberately absent and must\nnever be added: SkillKeeper's config is committed and synchronized, so a\nsecret placed here leaks by construction. A user with a confidential client\nsupplies the secret through their agent's own command, which stores it in\nthe platform keychain.",
      "properties": {
        "callbackPort": {
          "description": "Fixed loopback port for the redirect URI. Absent lets the agent choose.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "clientId": {
          "description": "Pre-registered OAuth client id. Absent leaves the agent to register\ndynamically.",
          "type": [
            "string",
            "null"
          ]
        },
        "scopes": {
          "description": "Requested scopes, canonically a list because the agents disagree on the\nwire type: Claude Code takes one space-separated string, Cursor and\nCodex take arrays. A list converts to both without loss; a string does\nnot, since splitting is not reversible when a scope contains a space.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "McpServerDef": {
      "allOf": [
        {
          "if": {
            "properties": {
              "type": {
                "const": "stdio"
              }
            },
            "required": [
              "type"
            ]
          },
          "then": {
            "properties": {
              "command": {
                "type": "string"
              }
            },
            "required": [
              "command"
            ]
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "enum": [
                  "http",
                  "sse"
                ]
              }
            },
            "required": [
              "type"
            ]
          },
          "then": {
            "properties": {
              "url": {
                "type": "string"
              }
            },
            "required": [
              "url"
            ]
          }
        }
      ],
      "description": "A single MCP server definition. `type` selects the transport; the remaining\nfields are populated per transport (`url`/`headers` for `http`/`sse`,\n`command`/`args`/`env` for `stdio`). `rules` carries optional free-form\nusage guidance.\nAny string field may carry `{name}` placeholders; their values are asked for\nat install time. Quote a value that STARTS with one -- a leading `{` opens a\nflow mapping in YAML, so `X-Token: {tok}` is a map, not text.",
      "properties": {
        "args": {
          "description": "Arguments passed to `command`, for `stdio`.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "command": {
          "description": "Executable to launch. Required for `stdio`; ignored otherwise.",
          "type": [
            "string",
            "null"
          ]
        },
        "env": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Environment variables for the launched process, for `stdio`. Quote any\nvalue that starts with a `{placeholder}`.",
          "type": [
            "object",
            "null"
          ]
        },
        "headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Request headers, for `http` and `sse`. Quote any value that starts with\na `{placeholder}`.",
          "type": [
            "object",
            "null"
          ]
        },
        "name": {
          "description": "Preset name, unique within the file. Becomes the basis of the installed\ninstance's name.",
          "minLength": 1,
          "type": "string"
        },
        "oauth": {
          "anyOf": [
            {
              "$ref": "#/$defs/McpOauth"
            },
            {
              "type": "null"
            }
          ],
          "description": "OAuth client configuration. Meaningful only for `http` and `sse`."
        },
        "rules": {
          "description": "Usage guidance written into the agent's guidance file on install, the\nsame way skill guidance is.",
          "type": [
            "string",
            "null"
          ]
        },
        "type": {
          "$ref": "#/$defs/McpTransport",
          "description": "Transport: `stdio`, `http`, or `sse`. Selects which of the fields below\napply."
        },
        "url": {
          "description": "Server URL. Required for `http` and `sse`; ignored for `stdio`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "name",
        "type"
      ],
      "type": "object"
    },
    "McpTransport": {
      "description": "The transport used to reach an MCP server. `stdio` launches a local\nprocess; `http` and `sse` connect to a URL.",
      "enum": [
        "stdio",
        "http",
        "sse"
      ],
      "type": "string"
    }
  },
  "$id": "https://github.com/lorem-dev/skillkeeper/releases/latest/download/mcp.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "MCP server presets declared by a repository, at its root or in a skill-group directory. See https://github.com/lorem-dev/skillkeeper/blob/main/docs/usage/mcp.md",
  "properties": {
    "$schema": {
      "description": "Optional: some editors attach a schema with this key instead of a yaml-language-server comment.",
      "type": "string"
    },
    "servers": {
      "description": "The MCP server presets this file declares.",
      "items": {
        "$ref": "#/$defs/McpServerDef"
      },
      "type": "array"
    },
    "version": {
      "const": 1,
      "description": "Schema version. Always 1."
    }
  },
  "required": [
    "version",
    "servers"
  ],
  "title": "SkillKeeper mcp.yml",
  "type": "object"
}
